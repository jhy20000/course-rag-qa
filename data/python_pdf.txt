Python 可以读取 PDF 文件，但通常需要借助第三方库。
常见的 PDF 读取库包括 PyPDF2、pdfplumber、PyMuPDF 等。
如果不安装第三方库，Python 标准库本身不能方便地提取 PDF 正文。
在课程资料问答助手中，支持 PDF 读取可以作为后续升级方向。
实现 PDF 读取后，程序可以把 PDF 内容提取成文本，再放入 RAG 检索流程中。
当前项目还没有真正实现 PDF 读取，只是把 PDF 支持作为后续计划。
