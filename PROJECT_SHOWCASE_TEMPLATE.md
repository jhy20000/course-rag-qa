# course-rag-qa 项目展示模板

## 核心功能

- 读取 `data` 文件夹下的本地课程资料
- 支持 `.txt` 和 `.md` 两种文本资料格式
- 自动把资料切分成可检索片段
- 根据用户问题提取关键词并检索相关片段
- 展示命中的来源文件、关键词、匹配分数和资料内容
- 基于命中资料生成简短回答
- 资料不足时拒绝回答，避免编造内容
- 自动把问答过程保存到 `history.txt`

## 学习收获

- 理解了 RAG 的基本流程：先检索资料，再基于资料回答
- 理解了大模型幻觉问题，以及为什么需要让回答有来源和依据
- 使用 Python 标准库完成本地资料读取、切分、检索和记录保存
- 学会了把论文中的思路转化成一个可运行的小项目
- 学会了用 README、截图和历史记录展示项目效果

## 版本进度说明

| 版本 | 阶段目标 | 主要成果 |
| --- | --- | --- |
| v1.0 | 单文件资料问答 | 读取 `course.txt`，完成最小版关键词问答和无依据拒答 |
| v2.0 | 多资料文件问答 | 支持读取 `data` 下多个 `.txt` 文件，并展示来源文件 |
| v3.0 | Markdown 和问答记录 | 支持 `.md` 文件，新增 `history.txt` 保存问答记录 |
| v4.0 | 检索质量优化 | 增加停用词过滤、匹配分数、命中关键词和改问建议 |
| v5.0 | 展示材料整理 | 优化 README，整理版本历史，生成项目展示模板 |

## 如何运行项目

```powershell
cd "C:\Users\贾鸿宇\Desktop\course_rag_qa"
python main.py
```

运行后按提示输入问题，输入 `q` 退出。

## 展示时可以这样介绍

这是一个 RAG 入门 Demo。项目重点不是复杂算法，而是把“先查资料，再回答”的思想做成一个能运行、能展示、能继续升级的 Python 小项目。它可以读取本地课程资料，检索相关片段，展示来源文件，并在资料不足时拒绝回答，从而减少凭空编造。
